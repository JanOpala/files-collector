from functions import set_local_directory



if __name__ == "__main__":
    set_local_directory()
    # print(os.getcwd())
    # args = parse_arguments()
    # paths = prepare_paths(dargs.miesiące, args.ni, args.pory_dnia)
    # generate_data(paths,  args.tryb, args.format)